@model IEnumerable<ProjetoDCPoint.Models.ocorrencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ponto.codPonto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.statusOcorrencia.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.justificativa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ponto.codPonto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.statusOcorrencia.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.justificativa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codOcorrencia_ }) |
            @Html.ActionLink("Details", "Details", new { id=item.codOcorrencia_ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codOcorrencia_ })
        </td>
    </tr>
}

</table>
